// RegEx:- RegEx means Regular expression is an object. Sequence of characters used for defining a search pattern.

const regex=new RegExp(/^a...s$/);
// console.count(regex)
console.log(regex.test('alias'))


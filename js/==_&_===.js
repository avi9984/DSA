// "==" & "===":- Both are the comparision oprator, "==" compair the value and "===" compair both value & type.

let a=2, b="2"
let c =a==b // compair the value "=="
console.log(c)// true

let d=a===b // compair both type & value "==="
console.log(d)// false
